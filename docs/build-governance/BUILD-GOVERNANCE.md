# سجل الحوكمة الحي — مسار نشر بصير للإنتاج

## عقد البناء — G0

**الهدف.** إتاحة نشر إنتاجي يدوي من GitHub من دون أن يحتاج المالك إلى
طرفية Hostinger أو كلمة مرور في كل إصدار، مع نسخة استرجاعية حقيقية قبل أي
تحديث لموديولات أودو.

**المستخدمون.** مالك المستودع هو من يبدأ النشر من GitHub. حساب النشر الآلي
محدود ولا يملك كلمة مرور أو صلاحية عامة إلى الجذر؛ يستطيع فقط رفع الحزمة
المؤقتة وطلب غلاف نشر مضبوط المعاملات.

**النطاق في هذه الشريحة.**

- GitHub Actions يدوي، من رأس الفرع `main` فقط وبعبارة تأكيد صريحة؛ الشريحة
  الأولى تقبل فقط SHA `5e73e6b` والموديول `baseer_native_spend` حرفياً.
- حساب `baseer_deploy` على VPS، مفتاح SSH مستقل، وغلاف جذر محدود وثابت المسار
  `/usr/local/sbin/baseer-production-deploy`.
- حزمة مصدرية قابلة للإثبات، قفل تزامن، نسخة PostgreSQL + filestore، مقارنة
  لقطة بيانات تشغيلية، تبديل `current` ذري، فحص صحة، واسترجاع DB/filestore
  والرمز عند الفشل.
- أول إطلاق عبر المسار: الموديول الآمن `baseer_native_spend` فقط عند SHA
  `5e73e6b`؛ لا يشمل الملفات غير الملتزمة الموجودة محلياً.

**خارج النطاق.** خريطة الحسابات التحليلية الموحدة، إلزام التوزيع التحليلي،
ترحيل الفواتير القديمة، موديولات الدفعات/الداش بورد غير الملتزمة، أو تغيير
أي قاعدة محاسبية.

**معايير القبول.**

1. زر التشغيل في GitHub يقبل `main` فقط ولا يعمل من push أو PR.
2. يفشل الغلاف قبل إيقاف أودو عند SHA أو بنية الحزمة أو سعتها غير الصحيحة.
3. قبل المسار الذي قد يغيّر DB يوقف أودو، ثم توجد نسخة استرجاع PostgreSQL
   وfilestore قابلة للفحص ولقطة قبل/بعد لبيانات الحركة المحمية من نفس لحظة
   السكون.
4. أي فشل بعد تبديل الإصدار يعيد symlink السابق، وعند بدء تحديث الموديول
   يعيد أيضاً قاعدة البيانات وfilestore ثم يثبت صحة الإصدار السابق؛ فشل
   الاسترجاع أو الصحة يعلن `ROLLBACK_FAILED` ولا يُعلن نجاحاً زائفاً.
5. تنقل الحزمة من صندوق الرفع القابل للكتابة لحساب النشر إلى حجر root-only،
   ثم تنسخ إلى inode جديد يملكه root قبل فحص hash أو المحتوى أو الاستخراج.
6. بعد النجاح يكون `/web/login` سليماً، والموديولات المطلوبة مثبتة، وسجل
   تشغيل منقح متاح في مجلد النسخة الاحتياطية.

### خريطة دورة ERP والرقابة

هذه الشريحة لا تنشئ أو تعدل مستندات مبيعات أو مشتريات أو دفعات أو قيود.
النقطة الوحيدة التي قد تمس قاعدة البيانات هي تثبيت/تحديث موديول أودو؛ لذلك
يوقف الغلاف أودو قبلها، ثم يقارن لقطة مستندات الحركة المحمية، ويستعيد الزوج
الموثق إذا تعذر القبول. لا تعتبر الإضافة التحليلية الجديدة ترحيلاً تاريخياً.

### الافتراضات المقيدة

- الإصدار الحي الحالي هو `f54d83b` ويطابق أساس المصدر الموثق في الإيصال.
- خدمة PostgreSQL وvolume الفلستر مرتبطان بأسماء Compose الحالية.
- حساب GitHub الحالي يملك صلاحية إدارة المستودع؛ مفتاح SSH الخاص سيبقى في
  GitHub Environment secret فقط ولا يوثق هنا ولا يرسل عبر المحادثة.

## ملف الإطلاق والنمو — G1

| بند | الدليل/الافتراض المحافظ |
|---|---|
| الرحلة الحرجة | طلب نشر واحد، رفع أرشيف ~44 MB، نسخ احتياطي، تحديث موديولات ثم `/web/login`. |
| التزامن | قفل `flock` واحد يمنع نشرين متزامنين؛ لا يمر إصدار ثانٍ قبل انتهاء الأول. |
| السعة الحالية | VPS: 96 GB، المتاح 14 GB تقريباً وقت الفحص؛ الشريحة تفحص مساحة تكفي زوج الاسترجاع + الحزمة + 1 GB قبل إيقاف أودو. |
| الاستمرارية | يوقف أودو أولاً ثم DB dump بصيغة custom + filestore tar + SHA-256؛ الخطأ بعد التحديث يستعيد الزوج والإصدار السابق بفحص صحة إلزامي. |
| زمن التعافي المستهدف | أقل من 15 دقيقة في الإصدار المعتاد؛ الصحة المحلية تحاول 90 ثانية قبل بدء الاسترجاع. |
| الرصد | logs لكل مرحلة في مسار النسخة الاحتياطية، نتيجة `LIVE=GO` أو `LIVE=ROLLED_BACK`، وسجل GitHub Actions. |

لا يُجرى اختبار حمل على الإنتاج. فحص السعة هنا حسابي وعمليات النشر متسلسلة؛
لا يثبت سعة استخدام التطبيق نفسه.

## معمارية التشغيل والعقود — G2

```text
مالك GitHub
  -> Run workflow (main + DEPLOY)
  -> GitHub hosted runner يبني git archive من SHA ثابت
  -> SSH key مخصص
  -> baseer_deploy:/deploy-inbox
  -> sudo محدود إلى baseer-production-deploy
  -> lock + validate + backup + atomic release + Odoo module action
  -> health / snapshot
  -> success أو restore كامل إلى الإصدار السابق
```

**مصدر الحقيقة.** SHA الذي يخرجه `git rev-parse HEAD` من checkout لـ`main`؛
الغلاف يقارن SHA-256 للأرشيف ويقبل المسار الصريح فقط. ملفات `.env` و
`config/odoo.conf` لا تدخل الأرشيف وتنسخ من الإصدار الحي إلى الإصدار الجديد.

**العقد.** الغلاف يقبل فقط:

```text
/srv/abroj-baseer-production/deploy-inbox/baseer-<40-hex>.tar.gz
<40-hex SHA> <GitHub run id> <64-hex archive SHA>
```

ويرفض الرموز/المسارات أو archive entries خارج `source/` أو symlinks/hardlinks.
يقتصر `sudo` على الغلاف الثابت ولا يأخذ قائمة موديولات من المستخدم؛ الغلاف
يثبت/يحدث `baseer_native_spend` فقط ويرفض كل SHA غير المعتمد للشريحة الأولى.
يُضبط known_hosts بمفتاح ED25519 المفحوص من VPS، ويمنع حساب النشر كلمة المرور
والـTTY والـforwarding، ولا يملك docker group أو sudo آخر.

**التزامن ومنع التكرار.** lock على مستوى الخادم؛ اسم الإصدار يحمل SHA وrun id؛
وإعادة تشغيل نفس SHA تنتج إصداراً مستقلاً مع نسخة احتياطية جديدة بدلاً من
الكتابة فوق الإصدار الحالي.

**الفشل والرجوع.** قبل التبديل كل شيء قابل للفشل بلا أثر. بعد السكون تؤخذ
اللقطة والزوج الاسترجاعي، ثم بعد التبديل يعيد trap symlink السابق؛ وإذا بدأت
عملية أودو يعيد dump وfilestore المأخوذين في السكون.
لا يبتلع فشل الاسترجاع أو فحص الصحة؛ يسجل `ROLLBACK_FAILED` ويخرج بفشل واضح.
لا يحذف نسخاً قديمة تلقائياً؛ التنظيف سياسة منفصلة بعد مراجعة السعة.

## التقنية والاعتمادات — G3

| القرار | البدائل | قرار المسار المباشر |
|---|---|---|
| تنفيذ النشر | طرفية Hostinger يدوية أو runner ذاتي | GitHub-hosted Actions عبر SSH؛ لا يحتاج runner مقيم أو جلسة متصفح، ويتطلب إعداداً واحداً للمفتاح والحساب المقيد. |
| حزمة المصدر | clone من VPS بمفتاح GitHub أو رفع ملفات يدوياً | `git archive` من SHA checked-out؛ لا يحتاج Git credential على الخادم ولا يضم `.env`. |
| حماية الوصول | root SSH key لعمليات Actions | مستخدم نشر منفصل + sudo لغلاف واحد ثابت؛ لا كلمة مرور ولا docker group، وknown_hosts صريح. |
| النسخ والاسترجاع | symlink فقط | PostgreSQL + filestore + symlink؛ يلزم لاستعادة أثر تثبيت موديول أودو. |
| مكتبات جديدة | أي action طرف ثالث للنشر | لا توجد؛ فقط `actions/checkout` الرسمي المثبت على SHA. |

**سؤال المسار المباشر.** نعم: هذا أقصر مسار يزيل اعتماد المالك على طرفية
الجوال من دون جعل VPS عاماً أو تخزين GitHub credential عليه، ويحافظ على نمط
الإصدار الذري والنسخ الموجود بالفعل.

## سجل البوابات

| البوابة | الحالة | المالك | المراجع المستقل | دليل الدخول/الخروج |
|---|---|---|---|---|
| G0 | معتمدة | قائد البناء/ERP | مراجع تشغيل مستقل | هذا العقد وخريطة دورة ERP؛ اعتماد المراجع بعد تضييق SHA/module. |
| G1 | معتمدة | مهندس الاستمرارية | معماري مستقل | جدول السعة + فحص VPS: DB ~163 MB، filestore ~69 MB، متاح ~14 GB. |
| G2 | معتمدة | مهندس التشغيل | ERP/استمرارية | العقد، حدود الصلاحية، مسار الاسترجاع؛ التنفيذ والبروفة مطلوبان قبل الإطلاق. |
| G3 | معتمدة | مهندس التشغيل | معماري مستقل | GitHub-hosted Actions وOpenSSH وCompose فقط، بلا مكتبة جديدة. |
| G4 | غير منطبق | — | — | لا واجهة تطبيق جديدة؛ GitHub واجهة المنصة القائمة |
| G5 | معتمدة | مهندس التشغيل | حارس الجودة | الحزمة، العزل، النسخ والاسترجاع، والإطلاق الفعلي مثبتة. |
| G6 | معتمدة | مهندس الاستمرارية | حارس الجودة | فحص السعة والإطلاق الفعلي جريا بنجاح؛ لا يتضمن ذلك اختبار حمل إنتاجي. |
| G7 | معتمدة | قائد البناء | حارس البوابات | الشريحة المحددة نُشرت، وسجلها ودليل صحتها محفوظان. |
| G8 | معتمدة | قائد البناء | فريق تسليم ألفا | المراجع المستقل أصدر GO للشريحة الآمنة فقط. |

## الشريحة التالية S2 — خريطة الإنفاق الأصلية

**الهدف.** تحويل تصنيف الموردين والمشتريات إلى خريطة أودو التحليلية الأصلية
القابلة للتدقيق، بحيث يحصل سطر فاتورة المورد على توزيع تحليلي صحيح قبل
ترحيله، ويستمد داشبورد المشتريات قراءته منه لاحقاً.

**مصدر الحقيقة.** خطة `تصنيف الإنفاق` وحساباتها التحليلية ونماذج التوزيع
التحليلي الأصلية في أودو. علامات تصنيف المورد هي مفتاح المطابقة عند غياب
الصنف؛ لا تصبح فئة مورد مخصصة ثانية ولا مصدر حقيقة موازٍ.

**حدود الشريحة.**

- تبنى شجرة مرجعية مشتركة من تصنيفات نوركس المعتمدة، ثم تنشأ لكل شركة
  الحسابات/النماذج الخاصة بها أو المشتركة بحسب عقد أودو.
- أولوية التوزيع هي: الصنف، ثم فئة الصنف، ثم المورد، ثم علامة تصنيف المورد.
  إذا أعطت العلامات المتعددة نتائج متضاربة، فلا يوجد اختيار صامت: يختار
  المستخدم التصنيف أو ينشأ نموذج أكثر تحديداً.
- لا يفعل الإلزام لشركة إلا بعد أن تملك شجرتها ونماذجها المعتمدة. عند التفعيل
  ترفض الفاتورة أو الإدخال الجماعي الترحيل من دون توزيع إنفاق كامل `100%`.
- الفواتير القديمة لا تعدل ضمن التهيئة؛ تبدأ بمعاينة ومصالحة ثم ترحيل منفصل
  قابل للمراجعة والعكس، للأسطر ذات المطابقة غير الملتبسة فقط.

**دليل البداية.** الجرد الحي أظهر خطة `تصنيف الإنفاق` وخمسة حسابات أوراق
حالياً وخمسة نماذج توزيع مرتبطة بموردين في الشركة النشطة؛ هذه تهيئة جزئية ولا
تمثل الخريطة الموحدة أو ترحيل الفواتير التاريخية.

| بوابة S2 | الحالة | الدليل المطلوب قبل الانتقال |
|---|---|---|
| G0 | مشروطة | التصميم/المعاينة معتمدان فقط؛ لا seed أو إلزام أو ترحيل حتى تغلق شروط التعارض والجاهزية والتاريخ. |
| G1 | قيد المراجعة | جرد الشركات والموردين والعلامات والفواتير غير المصنفة وحجم الدفعات مكتمل؛ يلزم اعتماد السعة ومسار الدفعات. |
| G2 | مسودة | عقد seed والمطابقة، منع التكرار، سجل المعاينة والترحيل والتراجع. |
| G3 | معتمدة مبدئياً | أودو التحليلي القياسي والموديول المنشور؛ لا مكتبة أو رصة إضافية. |
| G4–G8 | غير مبدوءة | تبدأ بعد اعتماد G0–G2 ومراجعة الخريطة الفعلية. |

### عقد S2 المقترح — G2

1. **الكتالوج.** شجرة نوركس هي كتالوج مُرقّم بإصدار، يتكون من خطط أبناء
   لحسابات الفئات الأم وحسابات تحليلية للأوراق. الحساب المشترك يحمل
   `company_id=False` ويخدم كل الشركات؛ الاختلاف الحقيقي فقط ينشئ override
   خاصاً بالشركة، ولا تنسخ الشجرة خمس مرات.
2. **المطابقة.** ينشأ نموذج توزيع أودو قياسي واحد لكل قاعدة معتمدة. داخل
   المستوى نفسه: الخاص بالشركة قبل المشترك؛ ولا يقبل التنفيذ نماذج مكررة أو
   متساوية تعطي أوراق إنفاق مختلفة. الترتيب بين المستويات ثابت: الصنف، فئة
   الصنف، المورد، علامة المورد. تعدد العلامات مقبول فقط إذا انتهت كلها إلى
   الورقة نفسها.
3. **المعاينة والجاهزية.** يسجل النظام run غير قابل للكتابة بعد اعتماده،
   ولقطة المورد/العلامة/الشركة/القاعدة/الورقة المقترحة والنتيجة
   (`resolved` أو `ambiguous` أو `unmapped` أو `conflict`). لا تصبح الشركة
   جاهزة إلا إذا كانت حساباتها فعالة، وجميع علامات مورديها لها نتيجة واحدة،
   والموردون بلا علامات لديهم قاعدة مورد صريحة، وتمر رحلة الفاتورة والإدخال
   الجماعي في QA.
4. **التفعيل.** لا يفعّل الإلزام إلا بعد وجود قالب مشترك مكتمل من حسابات
   تحليلية مشتركة ونماذج توزيع Odoo مرتبطة بالعلامات. بعد تهيئة هذا القالب
   وفحصه، ترث الشركة الجديدة حارس الترحيل عند إنشائها؛ لا تنسخ نماذج أو
   حسابات لكل شركة ولا تبقى الشركة الجديدة اختيارية بالخطأ. المورد بلا علامة
   أو متعدد الوجهات يبقى مسودة قابلة للحفظ، ويرفض الترحيل حتى يكتمل تصنيفه.
5. **التاريخ.** لا تكتب أي فاتورة مرحلة في S2. شريحة التاريخ اللاحقة تلتقط
   hash لكل سطر ومصدر القاعدة والمبلغ والحساب المقترح، وتقبل النتائج الواضحة
   وحدها ضمن run idempotent وسجل تراجع مثبت في QA؛ لا تمس القيود أو المبالغ.

**معايير قبول كود المعاينة.** يعرّف المفتاح الطبيعي للقاعدة صراحةً كـ
`(catalog version, company scope, selector kind, selector id)` ويرفض تكراره ولو اتفقت الورقة؛
المعاينة تنشئ سياق سطر للصنف/فئته/المورد/العلامة وكذلك سطر بلا صنف؛ وتبطل
الجاهزية إن اختلفت لقطة المورد أو علاماته أو الصنف أو فئته أو القاعدة أو
الحساب بعد run. اعتماد run أو تغيير الجاهزية محصور في مدير المحاسبة وسجله
ثابت. المورد الجديد بلا علامة لا يحصل على اختيار افتراضي.

**حالات تتطلب نتيجة معاينة لا تخميناً:** `فحم` (له سياقان في كتالوج نوركس)،
`تأمين طبي`، `مقدمو الخدمات`، `رسوم تطبيقات`، `فواتير نقدية صغيرة` والعلامات
الأم العامة. تعالج بورقة محددة أو قاعدة مورد/صنف أدق بعد ظهورها في المعاينة.

## دفتر العمليات الحي

| المعرّف والوقت | البوابة | النوع | النية/التغيير | النتيجة والدليل | الأثر والتراجع | المنفذ/المراجع |
|---|---|---|---|---|---|---|
| OPS-001 — 2026-09-16 | G0–G3 | قراءة/قرار | فحص release receipt، GitHub وHostinger؛ اختيار GitHub-hosted Actions + مستخدم نشر محدود بدلاً من طرفية الجوال أو runner غير مسجل. | baseline `f54d83b`; المرشح الآمن `5e73e6b`; مساحة VPS 14 GB تقريباً؛ لا runner مرتبط بالمستودع. | لا تغيير حي. | قائد البناء؛ مراجعة مستقلة مطلوبة. |
| OPS-002 — 2026-09-16 | G0–G3 | مراجعة مستقلة | سجل مراجع التسليم أربع ملاحظات P1: تثبيت عناصر التنفيذ في commit، SHA/module allowlist، بروفة استرجاع زوجية، وإثبات حدود المفتاح. | تم تضييق عقد الشريحة: SHA وموديول ثابتان، no module input، known_hosts وقيود حساب محددة؛ يبقى التنفيذ والبروفة دليلاً مطلوباً. | لا تغيير حي. | مراجع التسليم المستقل. |
| OPS-003 — 2026-09-16 | G0–G3 | اعتماد | أعاد المراجع المستقل فحص العقد بعد التضييق. | G0–G3 معتمدة للبناء؛ GH-01/GH-03 تنتقلان كدليل تنفيذ وبروفة قبل الإطلاق. | لا تغيير حي. | حارس البوابات/مراجع التسليم. |
| OPS-004 — 2026-09-16 | G5 | مراجعة مستقلة/تصحيح | اكتشف مراجع التسليم أن النسخ كان يوصف قبل إيقاف أودو وأن الرجوع قد يخفي فشلاً. | عُدّل التصميم والتنفيذ: السكون قبل DB+filestore+snapshot، وبروفة تعيد الخدمة مباشرة بعد الالتقاط؛ فشل الرجوع صريح. | لا تغيير حي؛ إعادة مراجعة مطلوبة. | مراجع التسليم/مهندس التشغيل. |
| OPS-005 — 2026-09-16 | G5 | مراجعة مستقلة/تصحيح | اكتشف مراجع التسليم نافذة TOCTOU: ملف الحزمة في صندوق رفع حساب النشر كان يفحص ثم يستخدم من المسار نفسه. | صار الغلاف يحجر inode نقلاً ذرياً ثم ينسخ bytes إلى inode جديد ينشئه root، ويفحص SHA ويستخدم النسخة الجديدة وحدها. | لا تغيير حي؛ إعادة مراجعة مطلوبة. | مراجع التسليم/مهندس التشغيل. |
| OPS-006 — 2026-09-16 | G5 | تنفيذ/تنظيف | تهيئة حساب GitHub المحدود توقفت أولاً بسبب خيار SSH غير مدعوم؛ أزيلت الحالة الجزئية من console موثوق قبل الإعادة. | `BOOTSTRAP_CLEAN=GO`، ثم `DEPLOY_ACCESS=READY`؛ الحساب بلا كلمة مرور/TTY/forwarding، sudoers وstaging مفحوصة، وصحة أودو سليمة. | لم تتغير بيانات أودو ولا المصدر الحي؛ الحساب المقيد هو مسار التراجع التشغيلي. | مهندس التشغيل؛ مراجع تسليم مستقل. |
| OPS-007 — 2026-09-16 | G5 | بروفة/تحقق | إيقاف قصير لأودو لالتقاط زوج DB+filestore متسق، ثم استعادة وتشغيل في DB وvolume معزولين. | `REHEARSAL=GO` و`REHEARSAL_VERIFY=GO`؛ checksums صحيحة، موارد البروفة نظفت، و`/web/login` الحي سليم. | لا تعديل لمستندات أو قيود حية؛ النسخة الاحتياطية محفوظة لمسار الاسترجاع. | مهندس التشغيل؛ مراجع تسليم مستقل. |
| OPS-008 — 2026-09-16 | G5 | فشل مضبوط/تصحيح | فشل تثبيت الشريحة الآمنة الأولى بسبب أن فك الحزمة تحت `umask 027` ترك مسار الإضافات غير قابل للعبور من مستخدم أودو داخل الحاوية. | التقط الغلاف `PermissionError` ثم استعاد الإصدار السابق آلياً: `LIVE=ROLLED_BACK` و`HEALTH=GO`؛ لم تتغير بيانات الأعمال. أضيف تصحيح يجعل مصدر الإصدار فقط قابلاً للقراءة والعبور قبل نسخ الأسرار بالأنماط المقيدة. | لا محاولة ثانية قبل مراجعة مستقلة؛ التراجع والنسخة الاحتياطية محفوظان. | مهندس التشغيل؛ مراجعة تسليم مستقلة مطلوبة. |
| OPS-009 — 2026-09-16 | G5 | فشل مضبوط/تصحيح | بعد تصحيح مسار الإضافات، وصل الغلاف إلى تهيئة أودو لكنه اكتشف أن `odoo.conf` الجديد نُسخ `0640 root:root` بينما الحاوية تعمل `odoo:odoo`؛ الإصدار الحي يثبت نفس ملف الإعداد بـ`0644`. | التقط الغلاف الخطأ ثم أعاد الإصدار السابق: `LIVE=ROLLED_BACK` و`HEALTH=GO`. عُدّل نمط `odoo.conf` إلى `0644` ليطابق الحي؛ `.env` يبقى `0600` ولا يدخل الأرشيف. | لا محاولة ثالثة قبل إعادة مراجعة مستقلة؛ نسخ الاسترجاع محفوظة. | مهندس التشغيل؛ مراجعة تسليم مستقلة مطلوبة. |
| OPS-010 — 2026-09-16 | G5 | مراجعة مستقلة/تصحيح | رفض المراجع المستقل `0644` لأن `odoo.conf` يحوي `admin_passwd`، ولأن توافق الإصدار الحي لا يبرر توسيع قراءة السر إلى حساب النشر. | الحاوية الحالية تعمل `uid=100(odoo),gid=101(odoo)` وحساب `baseer_deploy` في مجموعته الخاصة `1003` فقط. الغلاف يستخرج numeric GID من الحاوية الحية ويثبت `odoo.conf` بـ`root:<gid>,0640`؛ `.env` يبقى `0600`. | لا محاولة ثالثة قبل مراجعة التصحيح الدقيق؛ لا تغير حي من هذا التصحيح. | مراجع تسليم مستقل/مهندس التشغيل. |
| OPS-011 — 2026-09-16 | G5–G7 | إطلاق مضبوط | أعيد استخدام أرشيف root-only المتحقق منه، بعد نسخه إلى inbox كـinode جديد ومراجعة SHA والغلاف وحساب النشر. | المحاولة النهائية الوحيدة: `LIVE=GO`؛ release `5e73e6bb8b29-run-202609160500-20260916T010804Z`، commit `5e73e6b`، والموديول `baseer_native_spend` فقط. | النسخة السابقة محفوظة في `pre-5e73e6bb8b29-202609160500-20260916T010804Z`؛ لا تشمل الشريحة الملفات المحلية غير الملتزمة أو موديولات أخرى. | مهندس التشغيل. |
| OPS-012 — 2026-09-16 | G5–G8 | قبول مستقل | تحقق ما بعد الإطلاق من current، `/web/login`، حالة الموديول، لقطة البيانات المحمية، وعزل الأسرار. | `HEALTH=GO`؛ الموديول `installed`؛ `protected-before = protected-after`؛ `odoo.conf` هو `root:gid101,0640` وحساب النشر خارج gid101. أصدر مراجع التسليم GO. | لا تغيّر بيانات أعمال أو نقل تاريخي ضمن الشريحة. | مراجع التسليم المستقل. |
| OPS-013 — 2026-09-16 | S2 G0 | جرد/قرار | بدأ توسع خريطة الإنفاق بعد إطلاق S1. | الخطة الأصلية موجودة، لكن الحسابات والنماذج الحية جزئية ولا تصلح بعد لإلزام أو لمعالجة التاريخ. مصدر المطابقة المعتمد هو نماذج أودو التحليلية المرتبطة بعلامة المورد، مع أولوية الصنف قبلها. | قراءة فقط؛ لا تغير حسابات أو موردين أو فواتير. | قائد البناء/خبير ERP. |
| OPS-014 — 2026-09-16 | S2 G1 | جرد حي | قياس حجم الإطلاق قبل تصميم seed أو ترحيل. | 5 شركات؛ 281 مورداً نشطاً، 277 منهم بعلامات، و55 علامة مستخدمة. توجد 5 حسابات إنفاق و5 نماذج توزيع فقط في شركة واحدة. يوجد 2,962 سطر فاتورة مورد مرحلة بلا توزيع تحليلي. | قراءة PostgreSQL فقط؛ لا تغير سجلات. أي ترحيل تاريخي سيكون على دفعات مع معاينة ومصالحة. | مهندس السعة/خبير ERP. |
| OPS-015 — 2026-09-16 | S2 G0–G2 | مراجعة مستقلة | راجع مراجع التسليم عقد الخريطة قبل أي seed أو إلزام أو تاريخ. | GO مشروط للتصميم والمعاينة فقط. قبل التنفيذ: (1) كشف ورفض كل duplicate/tie في نماذج الصنف/الفئة/المورد/العلامة، مع company-specific قبل shared؛ (2) readiness معاين ومعتمد لكل شركة، لا إلزام تلقائي للشركة الجديدة؛ (3) التاريخ شريحة مستقلة بسجل دفعات ثابت، idempotency وتراجع مثبت في QA. | لا تغيير حي. النماذج الخمسة الحالية بيانات reconcile لا قالباً ينُسخ. | مراجع التسليم المستقل. |
| OPS-016 — 2026-09-16 | S2 G2 | تصميم | اعتمد قائد البناء عقد الكتالوج والمطابقة والمعاينة والجاهزية أعلاه، مستنداً إلى Odoo analytic accounting الرسمي. | لا dependency جديدة؛ Odoo يدعم خططاً هرمية وحسابات مشتركة ونماذج توزيع شرطية وإلزام applicability. ما زال العقد بانتظار المراجعة المستقلة قبل كتابة الموديول أو بيانات live. | لا تغيير حي. | قائد البناء/خبير ERP. |
| OPS-017 — 2026-09-16 | S2 G2 | إطلاق معاينة | نُشر مرشح المعاينة `e2897885` بعد GO مستقل؛ المراجعة أثبتت عزل الشركات، إصدارات الكتالوج، وثبات سجل الدليل. | `LIVE=GO`، الموديول `baseer_native_spend` مثبت، وفحص `/web/login` الحي يعيد 200. | الغلاف أخذ زوج استرجاع DB+filestore؛ لا seed للحسابات ولا applicability إلزامي ولا تعديل فواتير تاريخية. | قائد البناء/مراجع تسليم مستقل. |
| OPS-018 — 2026-09-16 | S2 G0–G2 | جرد/معاينة حية | فحصت القاعدة الحية بعد إطلاق المعاينة وسجلت run ثابتاً لكل شركة نشطة، من دون لمس أي فاتورة أو قيد. | 4 شركات نشطة، 0 قاعدة خريطة و0 نموذج توزيع حي عند البداية؛ كل runs محجوبة كما ينبغي لأن الكتالوج لم يزرع بعد. الكتالوج الحالي يحتوي 52 علامة ابن تحت ثماني فئات صرف وفئة الشيشة/التبغ؛ 26 مورداً خارج هذا الكتالوج أو بلا علامة. | سجلات معاينة فقط؛ لا تغيير مالي أو تاريخي. | قائد البناء/خبير ERP. |
| OPS-019 — 2026-09-16 | S2 G2 | قرار قالب | اختير أقصر مسار أصلي في أودو: حساب تحليلي مشترك لكل علامة ابن تحت خطتها الأم، ونموذج توزيع مشترك 100% لكل علامة. | النماذج المشتركة تمنع نسخ الخريطة بين الشركات وتطبق تلقائياً على الشركات الحالية والجديدة. العلامات المتعددة ذات الوجهات المختلفة، والعلامة العامة، والمورد بلا علامة لا تُخمّن؛ تبقى للمعالجة قبل الترحيل. الحسابات الخمسة الخاصة بالشركة القائمة لا تعدل ولا تدخل القالب. | لا تغيير بيانات حتى تنفيذ seed المراقب والتحقق. | قائد البناء/خبير ERP؛ مراجعة أساس مستقلة GO مشروطة. |
