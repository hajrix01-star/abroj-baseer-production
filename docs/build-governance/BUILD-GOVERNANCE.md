# حوكمة بناء ونشر بصير أودو

## الحالة السارية — 2026-09-16

هذا المستند هو المرجع التشغيلي الوحيد للنشر والاسترجاع. أزيلت منه العقود
التجريبية السابقة، وسياسات SHA الأحادية، ومسارات الأرشيف أو الطرفية اليدوية.
تاريخها محفوظ في Git ولا يصلح لتشغيل الإنتاج.

| المجال | الوضع الساري |
| --- | --- |
| مصدر الكود | `main` في `hajrix01-star/abroj-baseer-production` |
| بدء النشر | يدوي من GitHub Actions فقط |
| مدخل Action | `release_id` مسمى + كلمة تأكيد ثابتة فقط |
| قرار المكونات وcommit | سياسة إصدار محفوظة على الخادم بصلاحية `root:root,0600` |
| حساب النشر | `baseer_deploy` محدود بغلافي اعتماد/نشر ثابتين؛ لا root ولا Docker عام |
| مصدر الخادم | مفتاح GitHub للقراءة فقط محفوظ `root-only` |
| التزامن | قفل خادم مشترك بين الاعتماد والنشر |
| التراجع | الإصدار السابق + زوج PostgreSQL وfilestore متسق |

## العقد

```text
GitHub: اعتماد release_id
  -> الخادم يثبت سياسة الإصدار: commit كامل + موديولات مسموحة
GitHub: نشر release_id نفسه
  -> الخادم يتحقق من السياسة ويجلب المصدر بنفسه
  -> backup متسق -> ترقية محددة -> health أو rollback كامل
```

لا تستقبل الـActions أو الغلاف الخادمي SHA أو قائمة موديولات أو مسار أرشيف
من المستخدم؛ الإدخالان الوحيدان هما `release_id` وكلمة التأكيد الثابتة.
أي سياسة غير محمية، أو commit غير موجود على `main`، أو موديول
غير موجود في ذلك commit، أو نقص في السعة، يوقف العملية قبل توقف أودو.

## الدليل الحي

- آخر نشر عبر GitHub Actions: التشغيل
  [`35137590025`](https://github.com/hajrix01-star/abroj-baseer-production/actions/runs/35137590025)
  نجح في 2026-09-16.
- نُشر الإصدار المسمى `baseer-2026-09-16-branding` من commit
  `3b114e2ea349f7d7a0445a7a61b2698d026a7d3b`.
- تحققت صحة `/web/login` بعد النشر (`200`).
- القيد السابق لفوترة GitHub انتهى عندما جعل المالك المستودع عامًا؛ التشغيل
  الأحمر القديم سجل تاريخي فقط وليس حالة النشر الحالية.

## البيانات والتحليل

- مصدر تصنيف فواتير الموردين للوحة هو
  `account.move.line.analytic_distribution` المخزن مع السطر المحاسبي.
- لا تنشئ اللوحة أو Action أي فاتورة أو قيد أو توزيع تحليلي، ولا تعيد تخمينه
  من فئة المنتج أو علامة المورد أو الإدخال الجماعي.
- جرد 2026-09-16 للفواتير المرحلة ضمن النطاق المراجع وجد 2,962 سطرًا بتوزيع
  تحليلي و0 سطرًا بلا توزيع؛ لذلك أي `Unclassified` ظاهر لاحقًا يعالج أولًا
  كمسألة قراءة/تجميع للوحة، لا بتعديل تاريخ الفواتير تلقائيًا.

## النسخ والاحتفاظ

- قبل أي ترقية: زوج PostgreSQL + filestore وفحص سلامته.
- الخادم يحتفظ بالإصدار الحي، إصدار رجوع واحد، وآخر زوجين للاسترجاع.
- توجد نسخة محلية كاملة واحدة خارج المستودع للاسترجاع الطارئ. GitHub لا يحل
  محل نسخ البيانات.
- لا تحذف قاعدة البيانات أو filestore أو نسخة الرجوع عند تنظيف المصدر؛ تنظف
  فقط النسخ المؤقتة والمتكررة بعد التحقق من الزوج المحتفَظ به.

## بوابات التشغيل

| البوابة | الشرط | الحالة |
| --- | --- | --- |
| G0 | commit وموديولات معروفان في سياسة مسماة | مفعل |
| G1 | سعة كافية للإصدار وزوج الاسترجاع وهامش 1GB | مفعل |
| G2 | مفتاح المصدر والسياسة محميان وAction محدود | مفعل |
| G3 | قفل واحد ومسار مصدر واحد | مفعل |
| G4 | ترقية الموديولات المحددة فقط | مفعل عند النشر |
| G5 | صحة `/web/login` بعد الترقية | مفعل عند النشر |
| G6 | rollback تلقائي عند أي فشل بعد التغيير | مفعل عند النشر |

## أثر هذا التحديث

- لا تغيير للكود التشغيلي أو لبيانات أودو أو للصلاحيات.
- لا نشر مطلوب لهذا التغيير التوثيقي.
- المرجع المعماري: `BASEER-OPS-20260916` في
  `docs/architecture/registry/INDEX.md`.
